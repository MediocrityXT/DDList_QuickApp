{
  "name": "todos",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "start": "run-p watch server",
    "build": "hap build",
    "release": "hap release",
    "server": "hap server",
    "watch": "hap watch",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/",
    "debug": "hap debug"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^6.8.0",
    "eslint-plugin-ux": "^0.0.4",
    "hap-toolkit": "^0.7.5",
    "less": "^3.0.4",
    "less-loader": "^4.1.0",
    "npm-run-all": "^4.1.5"
  }
}
